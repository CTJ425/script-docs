# Knowledge-graph cache (local tooling)
.code-review-graph/

# Node
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Backups written by the installers (settings.json.bak.<timestamp>) and by hand
*.bak
*.bak.*

# Agent working directories (briefings, dispatches, handoffs). Scratch output
# of how a change was produced, not documentation of what it does.
.agents/
